import logger from 'winston';

export class Dom {
  static calculatePosition(dom, element) {
    // TODO try to use algorithm from https://github.com/keeganstreet/domtosource
    return { line: 0, column: 0 };
  }
}
